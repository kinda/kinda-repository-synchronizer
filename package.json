{
  "name": "kinda-repository-synchronizer",
  "version": "0.1.3",
  "description": "...",
  "repository": {
    "type": "git",
    "url": "git://github.com/kinda/kinda-repository-synchronizer"
  },
  "main": "index.js",
  "scripts": {
    "release": "(git checkout --quiet master && test -z \"$(git status --porcelain)\" && npm version $npm_config_release_type && git push --follow-tags) || echo \"An error occured (unclean repository?)\"",
    "release-patch": "npm run release --release-type=patch",
    "release-minor": "npm run release --release-type=minor",
    "release-major": "npm run release --release-type=major",
    "test": "mocha --harmony --ui tdd"
  },
  "author": "Manuel Vila <mvila@3base.com>",
  "license": "MIT",
  "dependencies": {
    "co": "^4.5.4",
    "co-wait": "0.0.0",
    "kinda-http-client": "^0.1.4",
    "kinda-log": "^0.1.10",
    "kinda-object": "^0.1.12",
    "lodash": "^3.5.0"
  },
  "devDependencies": {
    "chai": "^2.3.0",
    "co-mocha": "^1.1.0",
    "kinda-collection": "^0.4.1",
    "kinda-local-repository": "^0.3.3",
    "kinda-remote-repository": "^0.3.3",
    "kinda-repository-server": "^0.3.5",
    "koa": "^0.20.0",
    "mocha": "^2.2.4"
  }
}
